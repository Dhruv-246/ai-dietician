# Kamya Wellness onboarding voice agent (Pipecat + LiveKit). Python 3.10+ (tested on 3.11).
# Pinned to the version the bot was written/validated against.
pipecat-ai[sarvam,groq,silero,livekit,google,cartesia,aws]==1.7.0

# LiveKit browser-token minting + rtc client used by the transport.
livekit
livekit-api

# Web server that serves the call UI and mints join tokens.
fastapi
uvicorn

# Read the user's manual-onboarding profile from the shared Google Sheet.
gspread>=6.0.0
google-auth>=2.0.0

# P-3 thread machine: routes each turn and drives one conversational
# thread. Runs inside ONE Pipecat processor; it never owns the turn
# lifecycle or the generation call.
langgraph

# AWS Bedrock. The CONVERSATION model runs through pipecat's own
# AWSBedrockLLMService (the `aws` extra above), which needs boto3. The three
# small JSON jobs -- router, node check, consolidation -- skip boto3 and post
# to the InvokeModel endpoint over plain HTTPS with a bearer token, because a
# Bedrock API key is a bearer token and needs no SigV4 signing.
boto3

# Loads kamya-onboarding/.env so tests/test_chat_live.py can run against
# the real model locally without exporting variables by hand.
python-dotenv
reportlab==5.0.1
